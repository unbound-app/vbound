name: Check Version Bump

on:
    push:
        branches: [main]

jobs:
    check:
        runs-on: ubuntu-latest
        outputs:
            bumped: ${{ steps.check.outputs.bumped }}
        steps:
            - name: Checkout code
              uses: actions/checkout@v7
              with:
                  fetch-depth: 2

            - name: Check version bump
              id: check
              run: |
                  CURRENT=$(grep -m1 'MARKETING_VERSION' vbound.xcodeproj/project.pbxproj | tr -d ' ;' | cut -d= -f2)
                  PREVIOUS=$(git show HEAD~1:vbound.xcodeproj/project.pbxproj | grep -m1 'MARKETING_VERSION' | tr -d ' ;' | cut -d= -f2)
                  echo "Current: $CURRENT, Previous: $PREVIOUS"
                  if [ "$CURRENT" != "$PREVIOUS" ] && [ "$(printf '%s\n' "$PREVIOUS" "$CURRENT" | sort -V | tail -n1)" = "$CURRENT" ]; then
                    echo "bumped=true" >> "$GITHUB_OUTPUT"
                  else
                    echo "bumped=false" >> "$GITHUB_OUTPUT"
                  fi

    release:
        needs: check
        if: needs.check.outputs.bumped == 'true'
        uses: ./.github/workflows/release.yml
        secrets: inherit
        permissions:
            contents: write
